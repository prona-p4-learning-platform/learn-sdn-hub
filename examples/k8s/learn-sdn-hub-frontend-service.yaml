apiVersion: v1
kind: Service
metadata:
  name: learn-sdn-hub-frontend
  namespace: learn-sdn-hub
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/instance: learn-sdn-hub-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
