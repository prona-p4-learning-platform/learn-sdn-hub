apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "learn-sdn-hub.fullname" . }}-test-connection"
  labels:
    {{- include "learn-sdn-hub.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "learn-sdn-hub.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
